fastapi==0.109.2
ddgs>=6.0
uvicorn==0.27.1
pydantic>=2.8.0,<3.0.0
# D-205: was pinned ==2.2.1, which made the file unresolvable once
# langchain-community (>=2.4.0) was declared. Loosened to match the range the
# monolith already uses rather than dropping the package the code imports.
pydantic-settings>=2.4.0,<3.0.0
sqlalchemy>=2.0.25
asyncpg>=0.29.0
psycopg2-binary>=2.9.9
aiosqlite>=0.17.0
# D-205: was pinned ==1.55.0, which every current langchain-openai rejects
# (0.2.12+ needs openai>=1.55.3). The image's resolver enumerated 0.2.12 → 0.3.2,
# found none installable, and gave up — while a newer local pip happened to reach
# back to 0.2.11 and "succeeded". Relying on a resolver finding one old compatible
# corner is not a dependency set; removing the contradiction is. Unpinned upward
# to match the monolith, whose requirements leave `openai` free and whose test
# suite runs against the resulting set.
openai>=1.58.1
langgraph
dspy-ai==2.5.43
# D-205: `litellm==1.50.0` removed — zero imports anywhere in this service, and
# the pin was a second brake on `openai` (1.50.0 predates the langchain-openai
# range above). dspy-ai depends on litellm and brings a version consistent with
# whatever else resolves, which is the only reason it was ever present.
prometheus-client>=0.20.0
tavily-python>=0.3.0
# D-182 (ISS-134): the search engine imports llama_index eagerly at startup
# (retriever.py: VectorStoreIndex; llama_retriever.py: NodeWithScore construction),
# but these packages were absent → container crashed with ModuleNotFoundError:
# 'llama_index' before serving. Package names mirror the monolith's proven set
# (requirements-ci.txt). The reranker MODEL (sentence-transformers) stays optional
# via reranker.py's existing fail-open guard; the llama_index FRAMEWORK is structural.
llama-index-core>=0.14.13
llama-index-embeddings-huggingface
llama-index-vector-stores-supabase
# D-205: the same defect class as the D-182 comment above, found the same way it
# should have been found then — by building the image and importing the app.
# `main.py:21` imports super_search.py, whose module-level imports include
# BeautifulSoup, DuckDuckGoSearchAPIWrapper and ChatOpenAI. None of the three
# packages were declared, so the container built green and died on startup with
# `ModuleNotFoundError: No module named 'bs4'`. Fixing D-182 one package at a
# time did not work; nothing was building the image, so the next missing import
# just waited. langchain-core is already pulled in by langgraph.
beautifulsoup4>=4.12.3
langchain-community>=0.3.0
langchain-openai>=0.2.0
