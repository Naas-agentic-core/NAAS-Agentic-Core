| Execution Node | Trace Outcome | Root Cause Identified | File:Line | Exact Microscopic Fix Implemented |
|----------------|---------------|-----------------------|-----------|-----------------------------------|
| WS Stream Loop (Orchestrator) | Yielding raw `assistant_error` dict to final output | The Error Bleed (concatenating dicts as strings and forwarding error text to the UI) | `microservices/orchestrator_service/src/api/routes.py:618` | Intercepted `assistant_error`, sent directly via WS, and prevented overwriting `final_content` which gets persisted as a standard assistant message. |
| WS Stream Loop (Monolith Adapters) | Concatenating payload content from error chunks to `complete_ai_response` | The Error Bleed | `app/api/routers/customer_chat.py:231`, `app/api/routers/admin.py:303` | Wrapped text accumulation in `if event_type not in {"assistant_error", "error"}:` check. |
| Chat Streamer Loop | Direct pass-through appended error messages to full response | The Error Bleed | `app/services/customer/chat_streamer.py:62` | Wrapped append in check for error types. |
| Tavily Tool & Simple Scrapers | Sync blocking call `to_thread` without strict timeout. | Event Loop Starvation | `microservices/research_agent/src/search_engine/super_search.py:224-250`, `283-295` | Wrapped `asyncio.to_thread` with `asyncio.wait_for(..., timeout=10.0/15.0)` to enforce strict timeouts. |
| Research Client | Node circuit breaker missing | Crashes orchestrator upon Tavily/Search API timeout or failure | `microservices/orchestrator_service/src/infrastructure/clients/research_client.py:69` & `app/infrastructure/clients/research_client.py:66` | Added `try/except` wrapper to return graceful fallback string guiding LLM to use internal knowledge instead of raising exception. |