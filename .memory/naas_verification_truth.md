# 🔬 NAAS Verification Layer — الحالة الصادقة

> **الحالة تعيش هنا؛ القانون يعيش في**
> [`naas_verification_constitution.md`](naas_verification_constitution.md) +
> [`../docs/architecture/NAAS_VERIFICATION_LAYER.md`](../docs/architecture/NAAS_VERIFICATION_LAYER.md)
> + [`../docs/governance/GATE_STATE_MACHINE.md`](../docs/governance/GATE_STATE_MACHINE.md).
> تحرسها `scripts/fitness/check_naas_verification.py`: خانةُ فجوةٍ فارغة ⇒ CI أحمر ·
> دليلٌ لا يوجد ⇒ CI أحمر · وحدةٌ `ABSENT`/`PLANNED` ولها كود ⇒ CI أحمر ·
> حذفُ صفٍّ بصمت ⇒ CI أحمر.
>
> **قرار السجلّ:** D-267. **آخر تحقّق:** 2026-08-23.

---

## 1. لماذا هذا الملفّ موجود

الوثيقتان اللتان أقرّتا هذا الدستور تصفان منتجاً **لم يُبنَ بعد**. وأخطر ما يمكن أن
يحدث لوثيقةٍ كهذه أن تُقرأ بعد شهرٍ كأنّها تصف نظاماً قائماً.

⛔ **القاعدة الحاكمة لكل صفٍّ أدناه:** الطموح يُصنَّف ولا يُكتَم (D-209)، والترقية
بالبرهان الثلاثي (§6.6) لا بالكتابة. و`PLANNED` هنا **إعلانُ نيّةٍ متتبَّعة**، لا ادّعاءَ
قدرة.

## 2. سُلَّم الحالة (نفس سُلَّم §6.6 — لا سُلَّم ثانٍ)

`ACTIVE` · `PARTIAL` · `DORMANT` · `ZOMBIE` · `PLANNED` · `SEAM` · `ABSENT`.

---

### 2.a — وحدات طبقة التحقّق

| # | الوحدة | المسار المحجوز | دليل | الحالة | الفجوة الصادقة | شرط الترقية |
|---|--------|-----------------|------|--------|----------------|--------------|
| 1 | القلب المستقلّ عن المجال | `naas_verifier/core/` | `naas_verifier/core/verdict.py` | `ACTIVE` | مُنفَّذ ومُختبَر على مساراتٍ اصطناعية ومسابر الذخيرة — ⛔ ولم يقرأ بعدُ مسارَ وكيلٍ حقيقيّ تحت الاختبار | أوّل مسارٍ من نظام طرفٍ ثالث يُقرأ ويُحكَم عليه، بدليلٍ مُسجَّل |
| 2 | مُحوِّل المجال (السلّم) | `naas_verifier/adapters/` | `naas_verifier/adapters/multilingual_probe.py` | `PARTIAL` | طبقة المحوّلات قائمة وفيها مُحوِّلٌ حيّ واحد (الصفّ 10)؛ ومُحوِّل **السلّم** نفسه لم يُبنَ ومشتقّاته غير مُصدَّرة كبيانات | تصديرُ ملفّ بيانات السلّم مُلتزَماً + قراءةٌ صريحة الفشل عند غيابه |
| 10 | مُحوِّل المسابر متعدّدة اللغات | `naas_verifier/adapters/multilingual_probe.py` | `tests/naas_verifier/test_corpus_and_probes.py` | `ACTIVE` | يقرأ الذخيرة **بياناتٍ لا استيراداً** ويبني قيود الأبعاد الخمسة؛ ⛔ ولا يقرأ بعدُ مساراً من نظامٍ خارجي | ربطُه بمسارٍ من وكيلٍ حقيقيّ تحت الاختبار، بدليلٍ مُسجَّل |
| 3 | ذخيرة أصناف الاختراق | `naas_verifier/corpus/` | `naas_verifier/corpus/ar_fr_exploit_classes.json` | `ACTIVE` | ٥ أصنافٍ متمايزة الجذر مُشتَقّة من الأرشيف — و**اثنان منها `publishable=false`** لأنّ مصدرَيهما (ISS-146 · ISS-150) ما زالا مفتوحَين | إغلاق ISS-146 وISS-150 ليصير الخمسة قابلاً للنشر |
| 4 | خطوط الأساس للمقارنة | `naas_verifier/baselines/` | `naas_verifier/baselines/english_only_suite.py` | `PARTIAL` | الأساس الإنجليزي الحتمي حيّ ومقيس؛ و**حَكَم النموذج غير مبنيّ** فالمقارنة اليوم مقابل أساسٍ واحد | تشغيل حَكَم نموذجٍ **للمقارنة لا للحكم** (L4) على نفس المجموعة وتسجيل الفارق خاماً |
| 5 | طبقة المعايرة القياسية | `naas_verifier/calibration/` | `.memory/naas_verification_truth.md` | `ABSENT` | لا اتّفاقٌ مقيس بين المُتحقِّق ومراجعٍ بشري | عيّنةٌ مُصنَّفة بشرياً + معامل اتّفاقٍ محسوب ومنشور |
| 6 | التحقّق الصوريّ المساعد | `naas_verifier/formal/` | `app/core/foundations/probability.py` | `SEAM` | الأسس الحتمية قائمة في المنصّة ولا مستهلك لها من مسار المُتحقِّق | استهلاكٌ حيّ من الـadapter + برهانٌ ثلاثي |
| 7 | حزمة الأدلّة والحوكمة | `docs/governance/` | `docs/governance/GATE_LEDGER.json` | `ACTIVE` | مبنيّة ومحروسة؛ ولا سجلّ دليلٍ واحد مُسجَّل بعد (وهذا صادقٌ لا نقص) | — مُرقّاة فعلاً |
| 9 | الأهداف المرجعية للمعيار | `naas_verifier/targets/` | `naas_verifier/targets/reference.py` | `ACTIVE` | ثلاثة أنماطٍ لكلّ صنف (`vulnerable`/`lucky`/`hardened`) تُعيد إنتاج الجذر **اصطناعياً** — ⛔ وهي ليست أنظمة عملاء، فالرقم يقيس البطارية لا أداءً على إنتاجِ غيرنا | أهدافٌ من أنظمةٍ خارجية (نماذج مفتوحة) تُضاف بجانبها، ويُسجَّل الفارق عليها منفصلاً |
| 11 | بيئات التعلّم المعزَّز فوق المُتحقِّق | `naas_verifier/envs/` | `naas_verifier/envs/ar_fr_guard_break.py` | `ACTIVE` | ثلاث مهامّ بمكافأةٍ حتمية (صفر نموذجٍ في مسار المكافأة · تباينٌ صفر) تُستدعى من `naas_verifier.cli env` وتحرسها ٨٩ اختباراً منها براهين سلبية؛ ⛔ والحُرّاس فيها **حُرّاسنا الاصطناعية** لا نظامُ طرفٍ ثالث، ولم يستهلكها مُدرِّبٌ خارجيّ بعد | استهلاكُها في حلقة تدريبٍ خارجية (أو قبولُها في مركزِ بيئاتٍ عام) بدليلٍ مُسجَّل |
| 12 | مسبار الحارس الخارجي | `scripts/research/probe_external_guard.py` | `docs/research/EXTERNAL_GUARD_PROBE.json` | `PARTIAL` | أوّل قياسٍ على نظامٍ **لم نكتبه** (`better_profanity` مفتوح المصدر): صنفٌ واحد من ثلاثة قابلٌ للقياس عليه ونتيجتُه `violated` في البُعد الوسطي، والآخران مُصرَّحٌ بسبب تعذّرهما. ⛔ وقيدُ اللغة **موثَّقٌ من صانع الهدف** فليس ثغرةً مكتومة؛ ⛔ والبُعد النهائي مُنتهَكٌ أيضاً فالقيمة هنا **تشخيصية لا كشفية**. ⚠️ **والتبعية خارجيةٌ عمداً** فلا يُعيد CI القياس — يختبر منطقَ الصدق وحده (٧ اختبارات، منها برهانٌ سلبي أنّ التقرير لا يُدهَس) | هدفٌ خارجيٌّ ثانٍ **متمايز الصنف** (لا صيغةٌ أخرى من نفس الجذر) + تسجيل النتيجة في سجلّ دليلٍ موقَّع بشرياً |
| 8 | تنفيذ الرقع العدائية في صندوق | `naas_verifier/execution/` | `app/services/agent_tools/sandbox/policy.py` | `ABSENT` | ⛔ **ليس نسياناً بل قفلاً**: توليدُ رقعةٍ بنموذجٍ ثمّ تنفيذها هو بالضبط ما يمنعه قفل D-187 قبل `M1→M4` | استكمال `M1→M4` (عقد قدرات · مسبار حيّ · ميزانيات وتدقيق · حوكمة) — لا التفافَ عليه |

### 2.b — بوّابات القرار الأربع

الحالة الحيّة الكاملة في [`../docs/governance/GATE_LEDGER.json`](../docs/governance/GATE_LEDGER.json)
(مصدرٌ واحد — لا نسخة ثانية هنا). وأربعتها اليوم `ABSENT`، وهي الحالة الابتدائية
الصادقة: لا دليل مُسجَّل بعد، و`ABSENT` **ليست فشلاً**.

### 2.c — فرضيات السوق (لا حقائق)

⚠️ الأرقام أدناه من وثائق المالك، وتُكتب هنا **موسومةً** لأنّها ستُقرأ بعد أسابيع:

- `PRICING HYPOTHESIS`: تدقيقٌ أوّلي في نطاق آلاف الدولارات المنخفضة للدفعة الواحدة.
- `PRICING HYPOTHESIS`: معيارٌ مخصّص أو بيئة + مُتحقِّق في نطاقٍ أعلى.
- `MARKET HYPOTHESIS`: المشتري الأوّل مختبرٌ متوسط أو فريق بيئات صغير، لا المختبرات الكبرى.

⛔ **ولا واحدة منها دليل تسعير.** الدليل الوحيد المقبول معاملةٌ مُسوّاة تحت `GATE_C`.

---

## 3. قاعدة التحديث

1. **بناء وحدة** ⇒ ترقية صفّها بالبرهان الثلاثي في **نفس** الـPR + إدخال في `decisions.md`.
2. **الوحدة 8 لا تُرقّى قبل `M1→M4`** — القفل قانونٌ (D-187) لا تفضيل.
3. **لا حذف صامت لأيّ صفّ**، ولا خانة فجوةٍ فارغة — الفراغ يُقرأ نجاحاً.
4. **لا ادّعاء تفوّقٍ أو خندقٍ أو ملاءمةِ سوق** قبل أن تُصبح بوّابته `CLEARED`.
