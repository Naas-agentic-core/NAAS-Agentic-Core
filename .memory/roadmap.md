
> **Note: Core Architectural Doctrine Update**
> This platform is a **Cognitive Lab / Thinking Engine**, not a traditional Chat Tutor.
> The chat interface is merely an assistive channel. The true core consists of the Interactive Canvas (Object UI), Cognitive Modeling, Error Memory, Adaptive Generation, and Simulation Engine.
> See `cognitive_lab_philosophy.md` for the foundational doctrine.

# 🧭 خارطة الطريق الثورية — النجمة القطبية (Single Source of Truth)
## 1. الرؤية (لماذا نبني هذا أصلاً)
**تحديث (2026-06-27):** تم تطبيق التقدم التعليمي القائم على الحالة الدائمة (Stateful Pedagogical Progression) في المسار الحي (Monolith). النظام الآن يلتزم بمبدأ 'التدخل الأدنى المفيد' ولا يسمح بالتكرار العشوائي، حيث يقرأ الحالة عبر `TutorState` ويوجه LLM بناءً على ذلك، محبطاً كل أشكال الضياع أو الانجراف في موضوعات أخرى عبر حراسة صارمة للنوايا.

> **هذا الملف هو المصدر التفصيلي الحيّ الوحيد** للرؤية الثورية وخارطة الطريق.
> CLAUDE.md §0.6 يُلخّصه ويشير إليه. القرارات التفصيلية في `.memory/decisions.md`
> (D-104→D-113)؛ الكوارث المُصلَحة في `.memory/issues.md`؛ السرد الكامل في CLAUDE.md §6.91→§6.97.
>
> آخر تحديث: **2026-07-29** | الفرع: `claude/documentation-system-update-i0td9p` (D-188/D-189)
> 🏗️ **العدسة المعمارية المُكمِّلة:** `agentic_runtime_doctrine.md` (D-146) — خريطة طبقات
> الـ Agentic Runtime مُقيَّمة بصدق حسب §6.6 (ACTIVE/PARTIAL/PLANNED/DORMANT).

---

## 1. الرؤية (لماذا نبني هذا أصلاً)

**المعيار الأعلى ليس الانبهار اللحظي، بل الاستقلال المعرفي.**

المنصّة تخدم 800,000+ طالب بكالوريا جزائري. الكارثة التي نحاربها اسمها **وَهْم
الطلاقة (Fluency Illusion)**: الطالب يقرأ حلاً كاملاً مُعداً، يومئ، يشعر أنه «فهم»،
ثم ينهار يوم الامتحان — لأن الدراسة درّبت **التعرّف (recognition)** بينما الامتحان
يطلب **التوليد (generation)**. هذا هو الخلط الموثَّق بين **الأداء اللحظي** و**التعلّم
الدائم** (Bjork).

**النتيجة المرجوّة:** نظام تعليمي **يُجبر الطالب على توليد كل خطوة يستطيعها بنفسه**،
يقود سقراطياً (أسئلة + تلميحات متدرّجة)، **لا يكشف نتيجة نهائية أبداً** (إلا في وضع
تحقق صريح)، ويقيس الإتقان **الحقيقي** (غير المدعوم/المؤجَّل) لا الوهم.

---

## 2. المبادئ الثابتة (Immutable — لا تُكسر بدون ADR)

| # | المبدأ | المرجع |
|---|--------|--------|
| P-A | **القاعدة الذهبية**: لا تكشف أبداً خطوةً أو نتيجةً يستطيع الطالب توليدها بنفسه. مهمّة النظام إيجاد حافّة قدرة الطالب والعمل خطوةً واحدةً وراءها (ZPD). | D-113 · §6.97 |
| P-B | **منع مطلق للنتيجة النهائية** في الشرح (لا `$$\boxed{}$$`، لا `P(A)=14/165`). وضع تحقق منفصل اختياري فقط. | D-113 |
| P-C | **microservices-only هي الاتجاه**: الخدمات المصغرة + الرسم الـ12-node هي القلب الإلزامي للتوليد. تعذّرها ⇒ خطأ صريح، لا fallback صامت. | D-112 · §6.96 |
| P-D | **القياس الصادق = فجوة الوهم**: نُحسّن على (القدرة غير المدعومة المؤجَّلة)، لا على التفاعل/الرضا اللحظي. | §6.97 |
| P-E | **كل قدرة AI = Skill**: مسؤولية واحدة + عقد Pydantic + مقاييس Prometheus + اختبارات + مُستهلك حيّ (لا ZOMBIE). | §0.5 · D-073 |
| P-F | **BKT هو الطبقة المعرفية الأساسية**: كل قدرة تكيفية تُبنى فوق `student_mastery_probability`، لا تُعيد اختراع تتبّع الإتقان. | D-074 |

---

## 3. القواعد العشر التنفيذية (من بحث «وَهْم الإتقان»)

مستندة إلى: **Bjork** (الأداء ≠ التعلّم؛ صعوبات مرغوبة) · **Roediger** (أثر الاختبار/
الاسترجاع) · **Kapur** (الفشل المُنتِج) · **Renkl/Atkinson** (تلاشي المثال المحلول).

1. **حلّ قبل الشرح**: اطلب من الطالب محاولةً قبل أي تلميح.
2. **سُلّم تلميحات متصاعد**: من الأعمّ (سؤال موجِّه) إلى الأخصّ (خطوة بفجوة) — لا قفزة للحل.
3. **بروتوكول «لم أفهم» تشخيصي**: شخّص أين تعثّر، أعطِ تلميحاً أدنى واحداً — **ممنوع إعادة اشتقاق الحل** (أكثر الردود تدميراً).
4. **تلاشي المثال المحلول (worked-example fading)**: كلما ارتفع الإتقان، أزِل خطوات وادفع الطالب لملئها.
5. **الشرح الذاتي (self-explanation)**: اطلب من الطالب أن يشرح «لماذا» كل خطوة.
6. **التفنيد بعد الخطأ (elaborated feedback)**: صحّح على مستوى الفكر/المفهوم الخاطئ، لا الحكم بصواب/خطأ.
7. **توليد لا تعرّف**: صمّم التفاعل ليطلب إنتاجاً، لا اختيار/قراءة.
8. **فصل الأداء المدعوم عن الإتقان الحقيقي** في القياس (BKT صادق).
9. **الواجهات تُظهر البنية والمنهج دون الأرقام النهائية**.
10. **اقصد الصعوبة المرغوبة**: الانزعاج المنتج علامة تعلّم، لا عيب UX يُزال.

---

## 4. خريطة المراحل (جدول الحالة)

> ✅ منجز ومُتحقَّق · 🚧 جارٍ · 📋 مخطّط · ⏸️ مؤجَّل

| المرحلة | الهدف | القرار | الحالة | الدليل |
|---------|-------|--------|--------|--------|
| M0 — أساس BKT | تتبّع معرفي بايزي مُخزَّن (append-only) | D-074 | ✅ | `bkt_engine.py` + `student_bkt_analytics` |
| M1 — بيداغوجيا تكيفية | قراءة الإتقان تقود عمق التدريس | D-104 | ✅ | `AdaptivePedagogySkill` (Skill #15) |
| M2 — العمود الفقري الإلزامي | microservices hard-fail (لا fallback صامت) | D-112 | ✅ | `ORCHESTRATOR_REQUIRED`؛ E2E 6/6 |
| M3 — مسار تعلّمي | LearningPathSkill فوق BKT + بطاقة | D-111 | ✅ | Skill #17 + `learning_path_card` |
| **M4 — سقراطية (ج1)** | doctrine سقراطي + أسئلة-فقط + AnswerRedactionSkill (المحلي) | D-113 ج1 | ✅ | Skill #18 · §6.97 |
| **M5 — سقراطية (ج2)** | حجب + سقراطية في الـ orchestrator + سُلّم الدعم الخماسي | D-113 ج2 | ✅ | `response_sanitizer` + `SynthesizerNode` + v1.1.0 |
| **M5.5 — البروتوكول السقراطي المنضبط** | ~~D-114 المثال المحلول المكشوف~~ (عُكِس — كارثة حيّة) ← D-115: بروتوكول سقراطي منضبط في الـ orchestrator (نوع المسألة + سؤال واحد + خطوة) + قفل المفهوم + مُطهّر مُصفّح؛ السلطة الوحيدة للخدمات المصغرة | D-115 (يَعكِس D-114) | ✅ | §6.99 |
| **M7 (مُقدَّم) — مُعلّم الاحتمالات البصري التفاعلي** | الاحتمالات = بصري حتمي كامل (terminate LLM ⇒ صفر هلوسة) + سؤال واحد/شاشة → تحقق → كشف جزئي (الطالب يبني الجواب) + مُطهّر U+0305/`ë` + قفل مفهوم [-60:] | D-116 | ✅ | §6.100 |
| **M6 — صدق BKT** | فصل الأداء المدعوم عن الإتقان الحقيقي (assisted vs unaided-delayed) + `scaffold_leak` + بوّابة الإتقان غير المدعوم | D-157 (المرحلة A) | 🚧 | §6.132 |
| ↳ M6-A محرّك الإتقان الصادق | إشارة صواب ثلاثية + منحنى نسيان متّصل (durable يرتفع بصدق) + تجاوز رمزي (D-155) + ثابت مضاد للوهم | D-157 | ✅ | §6.132 |
| ↳ M6-0 محرّك الدور المعرفي (الأساس) | `tutor_state.kc_progress` مصدراً وحيداً لقرار الدور + إصلاح المخطط الميت (كل الحالة الدائمة D-142→D-157 كانت no-op على الإنتاج) + `_cognitive_turn` (خلف علم) | D-158 | ✅ | §6.133 |
| ↳ M6-0-P2 الهياكل المعرفية الحقيقية | مخطط `kc_progress` مُهيكَل + FSM تصعيد دائم + graph بحواف (`diagnose_root`) + مرآة knowledge_nodes/edges حيّة على الإنتاج + محرّك متعدد العُقد (A→B) + **تفعيل `COGNITIVE_TURN_ENABLED` افتراضياً** (E2E حي كامل) | D-159 | ✅ | §6.134 |
| ↳ M6-0-P3 نيّة «اشرح اشتقاق الخطوة» + دمج محدود | كاشف data-driven (قيمة/لون→جزئية) + حارس تكرار شامل على الإنقاذ النهائي + الطالب العالق يتعلّم لا يدور + F2-قبل-S2 (الشرح يهزم التحقّق الرقمي) — يقتل تكرار «كيف حسبنا 4» ×3 | D-160 | ✅ | §6.135 |
| ↳ M6-B بوّابة الإتقان | التقدّم على durable لا assisted (مع M8) | — | 📋 | — |
| **M7 — واجهات بلا أرقام** | تدرّج بصري سقراطي تفاعلي لا يكشف الجواب النهائي إطلاقاً | — | 📋 | — |
| **M8 — وضع التحقق المنفصل** | «تحقق من حلي» — المسار الوحيد لكشف النتيجة، بطلب صريح | — | 📋 | — |
| **M9 — مقياس فجوة الوهم** | Prometheus/Grafana: الأداء المدعوم − القدرة المؤجَّلة (بثّ `cogniforge_tutor_illusion_gap` + القناتان + لوحة 180) | D-157 | ✅ | §6.132 |
| **M10 — هجرة الرسم S2–S4** | port المهارات الحتمية داخل الـ orchestrator graph (المونوليث = بوابة WS رقيقة) + CritiqueNode (تحقق ذاتي) + Mastery-Aware Orchestrator + Real Synthesis | D-108/109/110 (مُصمَّمة) | 📋 | §6.95/§6.96 |
| M11 — الصوت | TTS/STT بنماذج صوتية متقدمة | D-107 | ⏸️ مؤجَّل | §6.93 |

> **مسارٌ موازٍ — طبقة المنتج والمنصّة (D-193→D-203، 2026-08-01).** خارج M0–M11 عمداً: هذه
> الخريطة بيداغوجية، وتلك دورة حياة المتعلّم والبنية التحتية. التتبّع الكامل في
> `spec.md §17b`؛ الحالة الحيّة في `.memory/runtime_truth.md`. باختصار: مفهومٌ واحد
> (D-193) · جدولة FSRS موصولة بـBKT (D-194) · مواظبة بتقويم الطالب (D-195) · لوحة وليّ
> لا تقرأ الحلّ (D-196) · تحليلات تعترف بجهلها (D-197) · قسائم بلا بوّابة دفع (D-198) ·
> واجهة محكومة برموز وميزانية (D-199) · بحثٌ يُرتِّب ويُفسِّر (D-200) · ناقل أحداث
> بانضباطٍ مُختبَر بلا وسيط + خطط سير عمل (D-201) · سجلّ قدرات النماذج (D-202) · تكافؤ
> الحجب بين العقلين (D-203).

---

## 4.4. خريطة علوم الحاسوب — الفجوات المُعلَنة (D-207 · 2026-08-02)

`docs/architecture/CS_KNOWLEDGE_MAP.md` يُصرِّح لعشرين مجالاً حالتَه ودليلَه الملفّي،
وتحرسه `check_cs_knowledge_map` (حالةٌ أمام ملفٍّ غير موجود ⇒ CI أحمر). الفجوات التي
أعلنها، مرتَّبةً كبنودٍ قابلة للعمل — **بلا وعدٍ بتاريخ**:

| # | الفجوة | المجال | لماذا تهمّ |
|---|--------|--------|------------|
| CS-1 | الطوبولوجيتان (uvicorn ⇄ Docker) تختلفان في خمسة منافذ، والثماني خدمات التطبيقية **غير مُقلَعة في CI** | 8 · 12 | «البناء ليس تشغيلاً» — الصورة تُبنى ولا يُثبَت أنها تخدم (D-201/D-205) |
| CS-2 | عامل Temporal لم يتّصل قطّ رغم إقلاع الوسيط | 12 | حزمة الأحداث مُثبَتة حيّاً؛ سير العمل لا |
| CS-3 | OTLP بلا `OTEL_EXPORTER_OTLP_ENDPOINT` = بلا أثر خارج العملية | 18 | «ACTIVE (no-op)» ليست ACTIVE (§0) |
| CS-4 | `pgvector` غائب في البيئات النظيفة ⇒ ثلاثة جداول لا تُنشَأ | 15 | يُبلَّغ صراحةً ولا يُسقِط الباقي (D-207 قانون 8) |
| CS-5 | لا محرّك تجريب (A/B) رغم وجود الأحداث | 20 | فجوة الوهم تُقاس ولا تُقارَن بين بدائل |
| CS-6 | تعليم علوم الحاسوب **للطالب** كمادّة: `ABSENT` | — | قرارُ منتج لا ثغرة هندسية — المنصّة لبكالوريا الجزائر |
| CS-7 | `probability_tutor._norm` في الخدمة أضعف من `shared.intent.normalize` (لا تطوي الهمزات ولا التشكيل) | 13 | التكافؤ محروسٌ بالمرآة (D-208)، لكنّ توحيد المُطبِّعَين يُلغي الحاجة لسرد صيغتَي الهمزة |
| CS-8 | `content_retrieval_skill/intent_classifier.py` بلا نظيرٍ قانوني يُقارَن به | 13 | مُصنِّف محلّي لا مرآة؛ الإعفاء يحمل تصريح «بلا بوّابة تكافؤ» المنطوق (D-208 قانون 7) |

⛔ **البنود لا تُغلَق بتحرير سطر**: إغلاق أيٍّ منها يتطلّب البرهان الثلاثي (§6.6) وتحديث
الخريطة في **نفس** الـPR.

## 4.5. العدسة المعمارية — خريطة طبقات الـ Agentic Runtime (D-146)

> الرؤية الثورية: CogniForge **agentic runtime** — حلقة model→tool→append بسيطة، والقوة في
> **الطبقات حولها**. الجدول الكامل (13 طبقة مُقيَّمة بصدق) في `agentic_runtime_doctrine.md`.
> الخريطة البيداغوجية أعلاه (M0–M11) والخريطة المعمارية هنا **عدستان للنظام نفسه**.

| الطبقة | الحالة (§6.6) | الطبقة | الحالة (§6.6) |
|--------|---------------|--------|---------------|
| Configuration / Skills / Hooks / Memory / Verification / Observability | ✅ ACTIVE | Subagents / Knowledge / Planner / Reasoning / Context | 🟡 PARTIAL |
| Plugin loader (`app/core/registry/`) | 🔴 DORMANT (ZOMBIE — لا import حيّ) | CritiqueNode (D-109) · Evolution engine | 📋 PLANNED |

**قاعدة الصدق:** لا تُرفَع أي طبقة إلى ACTIVE دون البرهان الثلاثي (import + call chain +
runtime evidence). دَيْن موثَّق: `DOC-DEBT-001` (CLAUDE.md موسوعة لا عقد — مُسجَّل، غير مُعالَج).

### 4.5.أ — الآفاق الثلاثة: طموحٌ مُصانٌ بشرط ترقية (D-209 · 2026-08-03)

طبقات التنسيق التسع (`AGENTIC_ORCHESTRATION_DOCTRINE.md`) **تستوعب** الثلاث عشرة أعلاه —
عدستان لنظامٍ واحد لا سُلَّمان. أمّا الآفاق الثلاثة التي تختم المقالة المصدر فهي **رؤية
استشرافية لا نظامٌ مكتمل**، وتُسجَّل هنا مراحلَ مرقّمة **لئلّا يُنسى الطموح بدل أن يُدَّعى**:

| المرحلة | الأفق | الحالة | **شرط الترقية** (ما الذي يجعلها ACTIVE؟) |
|---------|-------|--------|------------------------------------------|
| **M14** | Evolution — النظام يتعلّم من كل عطبٍ ومراجعة | 📋 PLANNED (صفر كود) | محرّكٌ يقترح تغييراً **ويُقيَّم قبل تطبيقه** — لا تعديلٌ ذاتي بلا بوّابة. يبني على CritiqueNode (D-109، M10) وعلى فجوة الوهم مقياساً |
| **M15** | Organization — Knowledge→Skills→Agents→Humans→Customers | ⏸️ SEAM (مقعد موثَّق) | فريقٌ ثانٍ فعليّ + حدود ملكيةٍ مُصرَّحة في `route_ownership_registry.json`. اليوم: مالكٌ واحد، فالتقسيم سابقٌ لأوانه |
| **M16** | Civilization — امتداد النموذج خارج التعليم | 📋 ABSENT (رؤية) | مجالٌ ثانٍ حقيقي بمستخدمين حقيقيين. **تُكتب رؤيةً وتبقى رؤيةً** حتى ذلك — والمقالة المصدر نفسها تُميّزها عن الواقع |

⛔ **لا يُرقَّى أفقٌ منها بالكتابة** — البرهان الثلاثي شرطٌ هنا كما في أيّ قدرة (§6.6).
تحرس تصنيفَها `scripts/fitness/check_agentic_orchestration.py`: حذفُ أحدها بصمت ⇒ CI أحمر.

## 4.6. طبقة القيمة والإيراد — M17→M30 (D-210 · 2026-08-04)

> **القانون:** `docs/VALUE_DOCTRINE.md` + `docs/REVENUE_ENGINE_SPEC.md` ·
> **الحالة الحيّة:** `.memory/revenue_engine_truth.md` (المصدر الوحيد — لا يُنسَخ هنا) ·
> **الفارض:** `scripts/fitness/check_revenue_doctrine.py`.
>
> خارج M0–M11 عمداً: تلك خريطةٌ بيداغوجية، وهذه سلسلة القيمة. **الطموح يُصنَّف ولا
> يُكتَم** (D-209): كل صفٍّ هنا يحمل شرط ترقيةٍ صريح، وحذفُ أيّه بصمت ⇒ CI أحمر.

| المرحلة | الوحدة | **شرط الترقية** (ما الذي يجعلها ACTIVE؟) |
|---------|--------|------------------------------------------|
| **M17** | `README` يفتح بـETAALIM (D-210) | أوّل ملفٍّ يُقرأ يعرّف منصّة بكالوريا جزائرية بلقطة شاشة، وملفّات الحوكمة **تبقى** فصلاً ثانياً. **أعلى عائدٍ لكل ساعة في القائمة كلّها** |
| **M18** | القبض — `billing_service` (D-211) | عقدٌ موقَّع مع مزوّد + بيئة اختبار + `check_entitlement_single_source`. ⛔ لا عميل «جاهز للتفعيل» قبل العقد |
| **M19** | ✅ فجوة الوهم (D-212) | **مُنجَزة** — يبقى التقاط الثقة في الواجهة وتقرير صفحةٍ واحدة قابل للتصوير |
| **M20** | بنك العناصر (D-213) | `check_item_coverage` خضراء — وهي التي **تحسم قرار الدعوة المفتوحة** في العرض الحيّ |
| **M21** | التصحيح بالسلّم (D-214) | رقمنة سلالم ثلاث دوراتٍ + رسمٌ لا دوريّ + `check_barem_item_linkage` |
| **M22** | البرهان الرمزي (D-215) | **ADR لتبنّي محرّك الجبر الرمزي** (تبعيّة جديدة) + `check_symbolic_no_timeout_pass` |
| **M23** | التوقّع والمعايرة (D-216) | M20 أوّلاً + `CalibrationCard` منشورة + `check_forecast_calibration_label` |
| **M24** | أطلس الأخطاء (D-217) | سجلٌّ بدورة حياة أسبوعية + `verified_by_human` مُثبَتاً |
| **M25** | مُحسِّن الوقت (D-218) | M23 أوّلاً + `check_optimizer_constraints` (التشبيك + الميزانية) |
| **M26** | اقتصاد الاستدلال (D-219) | `Tier` مُعلَن + `cogniforge_cost_per_student_month_dzd` حيّاً + `check_tier_budget` |
| **M27** | عقد الموسم (D-220) | التزامٌ يوقّعه الطالب **ويستطيع فسخه** + بند في `SAFEGUARDING.md` |
| **M28** | قناة الوليّ (D-221) | مزوّد رسائل مُتعاقَد + `WeeklyDigest` مُختبَراً + بند الحدّ الأخلاقي |
| **M29** | المعيار العمومي (D-222) | M20 أوّلاً + `METHODOLOGY.md` يذكر المُوسِّمين واتفاقهم + نتائج النماذج العامة |
| **M30** | البوّابة الجامعة (D-223) | ✅ الأساس قائم (`check_revenue_doctrine`)؛ تكتمل بانضمام بوّابة كل وحدةٍ تُبنى |

**نقطة القرار الحاسمة — نهاية الأسبوع 6 من تسلسل D-210:** إن كانت `check_item_coverage`
**خضراء**، يُدخَل العرض الحيّ بالدعوة المفتوحة («اختاروا أي مادة وأي سؤال»). وإن كانت
**حمراء**، يُدخَل بالدعوة المُضيَّقة الصادقة. ⛔ **لا يُدخَل بدعوةٍ مفتوحة وبوّابةٍ حمراء**
— هذه هي الطريقة الوحيدة لخسارة جلسةٍ كانت ستُكسَب.

⛔ **لا تُرقَّى مرحلةٌ بالكتابة** — البرهان الثلاثي شرطٌ هنا كما في أيّ قدرة (§6.6).

## 4.7. محرّك التنفيذ المعرفي + تسلسل السوق — M31→M44 (D-224/D-225 · 2026-08-04)

> **القانون:** `docs/architecture/COGNITIVE_EXECUTION_ENGINE.md` ·
> **الحالة الحيّة:** `.memory/cognitive_execution_truth.md` (المصدر الوحيد — لا يُنسَخ هنا) ·
> **الفارض:** `scripts/fitness/check_cognitive_execution.py`.

**الجملة الحاكمة:** الحقيقة تُنتَج بمحرّكٍ حتمي يُنفَّذ ويُتحقَّق منه، واللغة تصفها ولا
تُقرّرها. و⛔ **الحتمي أوّلاً والتوليد البرمجي آخِراً**: `REGISTRY → FOUNDATIONS →
SYMBOLIC → SYNTHESIS`.

| المرحلة | الطبقة | **شرط الترقية** |
|---------|--------|------------------|
| **M31** | الفهم اللغوي (1) | ✅ حيّة — يبقى مجموعة اختبارٍ موسومة تقيس الفهم عبر اللغات الثلاث |
| **M32** | الرسم المعرفي (2) | أنماط الخطأ تصير عُقَداً في الرسم (يشترط D-217) |
| **M33** | تصنيع البرنامج (3) | ⛔ **مقفول**: M1→M4 من §6.5.ج أوّلاً. لا استثناء ولا «تجربة سريعة» |
| **M34** | الصندوق الآمن (4) | ✅ مبنيّ ومُشدَّد — الترقية إلى ACTIVE **تعني** M1→M4، لا مستهلكاً ذكياً قبلها |
| **M35** | التحقّق الصوري (5) | ADR لتبنّي محرّك جبرٍ رمزي + `check_symbolic_no_timeout_pass` (D-215) |
| **M36** | التحويل التربوي (6) | ✅ حيّ — يبقى إغلاق المرحلة 12 من مراحل الدور (ISS-148) |
| **M37** | النموذج المعرفي (7) | ✅ حيّ — يبقى التقاط الثقة المُعلَنة في الواجهة (D-212) |
| **M38** | الذاكرة طويلة المدى (8) | ✅ حيّة — يبقى ربط «لماذا أخطأ» برسم الذاكرة (D-217) |
| **M39** | التخطيط التكيّفي (9) | دالّة هدفٍ عبر المواد + قيد تشبيك مفروض (D-218) |
| **M40** | نموذج العالم (10) | بيانات جلساتٍ حقيقية بحجمٍ كافٍ. **يُصنَّف ولا يُدَّعى** |
| **M41** | مجتمع الوكلاء (11) | `Critic` مستقلٌّ حيّ (D-109 · M10) — والكاتب لا يُصحّح نفسه |
| **M42** | التحسين الذاتي (12) | تقييمٌ قبل التطبيق + `check_agent_self_modification` (M4) |
| **M43** | المعرفة المُتحقَّقة (13) | اكتمال الحلقات 3 و5 — فالسلسلة لا تُغلَق بحلقةٍ مفقودة |
| **M44** | آفاق السوق (21→24) | الجزائر مُثبَتة ⇒ الفرنكوفونية ⇒ أوروبا ⇒ الولايات المتحدة. **كلٌّ بدليلٍ مقيس** |

### القفل الأمني — أهمّ سطر في هذا القسم

⛔ **M33 (تصنيع البرنامج) لا يُفتَح قبل M1→M4** (`§6.5.ج`). الصندوق مبنيٌّ ويشغّل
`python`/`pip`/`git`/`npm`، والمستخدمون **قاصرون**. توصيلُ نموذجٍ لغوي به بلا عقد قدرات
ولا ميزانية ولا سجلّ تدقيق هو الطريق المعروف إلى حادثةٍ أمنية. **القدرة ≠ الأمان.**
ويفرضه بند AST داخل `check_cognitive_execution`: وحدةٌ تجمع مُنفِّذ الصندوق مع عميل نموذج
⇒ CI أحمر (خمس تجارب سلبية مُثبَتة).

### تسلسل السوق (D-225)

الهدف أمريكا وأوروبا. والميزة هناك **ليست** «محادثةً أفضل» بل **محرّك تعلّمٍ معرفي قابل
للتحقّق**. ⛔ لكنّ السوق الأمريكي يشتري **الأدلّة**، والدخول بلا نتائج تعلّمٍ منشورة
ومعيارٍ عمومي (D-222) يحرق الفرصة **مرّةً واحدة**. والخندق الجزائري لا يُصدَّر — يُصدَّر
المحرّك بعد أن يُثبِت أثره. فالمحرَّم الخامس قائم: **لا توسّع قبل رسوخ الجزائر، والرسوخ
يُقاس ولا يُعلَن**.

## 4.8. التوأم الرقمي المعرفي — M45→M52 (D-226/D-227 · 2026-08-04)

> **القانون:** `docs/architecture/COGNITIVE_DIGITAL_TWIN.md` · **الحالة:**
> `.memory/cognitive_twin_truth.md` · **الفارض:** `check_cognitive_twin` +
> `check_prerequisite_single_graph`.

**الجملة الحاكمة:** الطالب **قصّةٌ مستمرّة** لا سلسلة أحداث مستقلّة. والألم المُعالَج
اسمه **كرة ثلج سوء الفهم**: ثغرةٌ مبكّرة لا يتذكّرها شيء تُسقِط عقلاً بعد سنوات.

| المرحلة | المحرّك | **شرط الترقية** |
|---------|---------|------------------|
| **M45** | رسم المنهاج (1) | ✅ حيّ — يبقى توسيع التغطية إلى منهاجٍ كامل |
| **M46** | رسمٌ واحد للعلاقة (2) | ✅ **أُصلح في D-226** — كان أعمى عن ٢٦ حافّة والتقاطع صفر |
| **M47** | تتبّع المعرفة BKT (3) | ✅ حيّ بقناتين — ⛔ لا DKT قبل حجم بياناتٍ ضخم |
| **M48** | التدخّل على الجذر (4) | ✅ حيّ عبر المواد — يبقى نصّ تدخّلٍ مكتوب لكل مفهوم منهاج |
| **M49** | التكرار المتباعد (5) | ✅ حيّ داخل المادة — يبقى **نسجُ مفهومٍ قديم داخل تمرينٍ جديد** |
| **M50** | البُعد الزمني (6) | حقل `term`/`month` في سجلّ المنهاج. ⛔ **حتى ذلك: لا ادّعاء توقيت** |
| **M51** | الذاكرة عبر السنوات (7) | انتقال الحالة عبر سنةٍ دراسية + هجرة مُعرَّفات بلا فقدٍ للتاريخ |
| **M52** | منهاج ثانٍ (فرنسي) | سجلٌّ يمرّ بوّابات D-193 نفسها + مُعرَّفات لا تصطدم بالمُخزَّن |

⛔ **حدّ المصداقية (D-227) يسري على كل مرحلةٍ هنا**: «يقرأ الأفكار» · «دقّة ١٠٠٪» ·
«صفر أخطاء» · «يغيّر البشرية» ممنوعة في الوثائق المُعلَنة. **عبارةٌ لا تُفنَّد دَينٌ لا
طموح** — تُصدَّق مرّة، ثمّ تُكتشَف، ثمّ يسقط معها كلُّ ما هو صحيح.

---

## 4.9. طبقة التحقّق (NAAS) — M53→M58 (D-267 · 2026-08-18)

> **القانون:** `.memory/naas_verification_constitution.md` +
> `docs/architecture/NAAS_VERIFICATION_LAYER.md` +
> `docs/governance/GATE_STATE_MACHINE.md` · **الحالة:**
> `.memory/naas_verification_truth.md` + `docs/governance/GATE_LEDGER.json` ·
> **الفارض:** `check_naas_verification`.

**الجملة الحاكمة:** الوكيل القويّ مع مُتحقِّقٍ ضعيف = **كذبٌ مُقنع**. ونحن نبني الطبقة
التي تُثبت أنّ الوكيل نجح فعلاً — أو تكشف أنّه تظاهر.

⛔ **وهذا الخطّ لا يستعير دليلَ المنتج التعليمي ولا يُعيره** (توضيح نطاق D-225): نتائجُ
تعلّمٍ طلابية ليست دليلاً على قدرة المُتحقِّق، والعكس صحيح. وأصولُ المنصّة (السلّم · بنك
العناصر · BKT/FSRS) **أصولٌ تقنية قابلة لإعادة الاستعمال لا moat تجاريّ مُثبَت**.

| المرحلة | الوحدة | **شرط الترقية** (يُقاس ولا يُعلَن) |
|---------|--------|-------------------------------------|
| **M53** | حزمة الحوكمة والأدلّة | ✅ حيّة — آلة الحالات + المخطّط + السجلّ + الفارض بـ٢٧ تجربة سلبية |
| **M54** | القلب المستقلّ عن المجال | أوّل شريحة رأسية تُنفِّذ الأبعاد الخمسة، بحالتَي نجاحٍ وفشلٍ لكلّ بُعد |
| **M55** | مُحوِّل السلّم | تصديرُ مشتقّات السلّم **بياناتٍ** + قراءةٌ صريحة الفشل عند الغياب (لا استيراد) |
| **M56** | خطوط الأساس والقياس | `Δ ≥ 15%` على أساسٍ ومجموعةٍ مثبَّتة بـ`runs ≥ 3` ⇒ سجلّ دليلٍ تحت `GATE_A` |
| **M57** | ذخيرة أصناف الاختراق | `≥ 3` أصنافٍ **متمايزة الجذر** بإعادة إنتاجٍ ومرجع مواصفة ⇒ `GATE_B` |
| **M58** | التحقّق التجاري | **معاملةٌ مُسوّاة** بمبلغٍ موجب ⇒ `GATE_C`. ⛔ نجومٌ واجتماعاتٌ وخطابُ نيّةٍ غير مدفوع **ليست دليلاً** |

⛔ **الوحدة الثامنة (تنفيذ الرقع العدائية) لا تُرقّى قبل `M1→M4`** — قفل D-187 قانونٌ لا
تفضيل، وتوليدُ رقعةٍ بنموذجٍ ثمّ تنفيذها هو **حمولةُ القفل** لا التفافٌ عليه.

⛔ **وحدّ المصداقية (D-227) يسري هنا أيضاً**، ويُضاف إليه **وسم الفرضية**: كل رقم سعرٍ أو
سوقٍ يحمل `PRICING HYPOTHESIS`/`MARKET HYPOTHESIS` — فرضيةُ سعرٍ ليست دليلَ تسعير.

---

## 5. تفاصيل المراحل المنجزة (✅)

### M4 — السقراطية ج1 (D-113 ج1 · §6.97)
- **doctrine سقراطي v3.0.0** (`doctrine.py:EXPLANATION_DOCTRINE`): القاعدة الذهبية +
  منع النتيجة النهائية + محاولة قبل الشرح + سُلّم تلميحات + بروتوكول «لم أفهم» + شرح ذاتي + تفنيد.
- **أسئلة-فقط للـ LLM**: `_stream_exercise_explanation_response` يمرّر `display_content`
  (أسئلة بلا حل) لا `full_content` — فلا يملك الـ LLM ما يكشفه.
- **`AnswerRedactionSkill` (Skill #18)** — حارس حتمي أخير: يحجب `\boxed{X}`، `P/E/C(...)=عدد`،
  أسطر «إذن…=عدد»، «الإجابة النهائية:». نطاق ضيّق يحفظ الصيغ التعليمية الرمزية.
  موصول في `content_integrity.sanitize_final_text` + `local_graph._apply_answer_redaction` + per-chunk.

### M5 — السقراطية ج2 (D-113 ج2 · §6.97)
- **port الحجب إلى الـ orchestrator** (`response_sanitizer.py`): `redact_final_answers` +
  `_strip_boxed` (stdlib مستقل — المونوليث لا يستورد من microservices). مدموج في
  `sanitize_response` (مخرَج كل عقدة) + `sanitize_chunk` (حجب `\boxed` الحيّ).
- **عقد السقراطية في `SynthesizerNode`** (`search.py`): prompts «اكتب الحل» → «قُد بأسئلة؛
  ممنوع كشف نتيجة لتمرين يحلّه الطالب؛ المعرفة العامة تُجاب مباشرة».
- **سُلّم الدعم الخماسي** (`adaptive_pedagogy_skill.py` → doctrine v1.1.0): `support_level` 1..5
  (worked_example → completion → backward_fading → prompted → unaided)، مشتقّ حتمياً من
  الإتقان + الحِمل، يصل الـ LLM عبر prepend الموجود.

---

## 6. تفاصيل المراحل المخطّطة (📋 — التتمة الثورية)

### M6 — صدق BKT (القياس الحقيقي)
الفجوة: BKT اليوم يرفع الإتقان حتى لو كان الأداء **مدعوماً بالكامل** (وهم). المطلوب:
- وسم كل تفاعل بـ `support_level` المُستخدَم فعلياً (من M5).
- تحديثان منفصلان: **assisted mastery** (مدعوم) و **unaided-delayed mastery** (غير مدعوم، مؤجَّل).
- إشارة `scaffold_leak`: ارتفاع الأداء المدعوم بلا ارتفاع غير المدعوم = وهم يُرصد.
- **بوّابة الإتقان غير المدعوم**: لا يُعلَن «أتقن» إلا بأداء غير مدعوم مؤجَّل.

### M7 — الواجهات بلا أرقام
الواجهات المولدة (probability_tree / combinations / full_exercise_story / math card)
تُظهر **البنية والمنهج** (الفروع، الصيغ الرمزية، خطوات المنهج) **دون الأرقام النهائية**؛
تدرّج بصري تفاعلي يطلب من الطالب ملء القيمة بنفسه. (يحترم P-B + P-A.)

### M8 — وضع التحقق المنفصل
المسار **الوحيد** لكشف النتيجة: طلب صريح «تحقق من حلي». يستقبل حلّ الطالب، يقارنه بالإجابة
النموذجية (`full_content` — لوضع التحقق حصراً حسب D-113)، ويُعطي تفنيداً مفصّلاً.

### M9 — مقياس فجوة الوهم (Illusion Gap)
مقياس Prometheus + لوحة Grafana: `illusion_gap = assisted_mastery − unaided_delayed_mastery`
لكل concept. هدف المنصّة تقليصه، لا تعظيم التفاعل. (يجسّد P-D.)

### M10 — هجرة الرسم S2–S4 (microservices-only الكاملة)
- **S2**: port المهارات الحتمية (probability / content_integrity / learning_path / greeting /
  indexed-retrieval + knowledge_base) **داخل** الـ orchestrator كعقد رسم — المونوليث يصبح
  بوابة WS رقيقة. «النقل» = port مستقل (نسخ)، لا import (المونوليث ولا microservices يستوردان من بعضهما).
  - **S2.1 ✅ منجز-خلف-علم (D-154، 2026-07-02)**: أول port حيّ —
    `microservices/orchestrator_service/src/services/overmind/probability_tutor.py` (stdlib مستقل:
    parse_composition محصَّن ضد نثر الحل + بوّابة مقامات LaTeX-aware + سُلّم كشف تدريجي صفر-نتيجة
    بحارس dedup) موصول في `SynthesizerNode` خلف `ORCHESTRATOR_PROB_TUTOR_ENABLED` (افتراضه "1" — **ACTIVE default-on منذ D-163 Stage 3**؛ رافعة الرجوع `=0`).
    الحالة الصادقة (§6.6): **FLAGGED** — يُقلب العلم بعد التحقق الحيّ في Codespaces.
  - **S2.4 ✅ الشارح المفاهيمي + تصعيد الحيرة على الـ port (D-173، 2026-07-21)**:
    `detect_conceptual_question` + `build_conceptual_relationship` (D-125: «ما الفرق بين 165 و 14»
    ⇒ العلاقة لا الحساب) — يُفحَص **قبل** step-explanation (نظير عقل المونوليث الذي يفحص المفاهيمي
    داخل `_build_probability_direct_explanation` قبل احترام forced_subpart) + تصعيد الحيرة F3
    (الطالب العالق يتعلّم اشتقاق جزئية لا يدور). بوّابة التكافؤ صارت **20 عقداً**.
  - **S2.3 ✅ «كيف حسبنا 4» تُعلَّم على الـ port (D-168، 2026-07-16)**: `detect_subpart_question`
    + `detect_step_explanation` + `build_step_explanation` (data-driven من `parse_composition`،
    صفر كشف للنسبة — يَنجو من حجب D-113) موصولة في `deterministic_turn` قبل بوّابة هروب
    الأسئلة (مرآة D-160 F2). بوّابة التكافؤ صارت **17 عقداً** — أُغلقت آخر فجوة split-brain
    حية من صنف ISS-126.
  - **S2.2 ✅ بوّابة تكافؤ split-brain (D-162، 2026-07-10)**: عقود المعلّم الجوهرية (بوّابة
    الفعل الكلامي «السؤال ليس إجابة» + مُجيب التسمية + dedup المحايد للحجب + `fmt_comb`)
    محروسة الآن في **النسختين** (monolith `orchestrator_client` + الـ port) عبر
    `check_pedagogical_os.check_split_brain_parity` — أي افتراق مستقبلي يُفشل CI
    (نمط D-013 الثنائي). المرآة المعرفية زُومنت حيّاً (22 عقدة/19 حافة، parity MATCH).
- **S3**: Mastery-Aware Orchestrator (P1, D-108) + Real Synthesis (P3, D-110 — `composition_confidence`).
- **S4**: CritiqueNode (P2, D-109) — عقدة تحقق ذاتي بعد Synthesizer (fail-open، علم بيئي).
- **قاعدة الصدق**: لا تُعلَن أيٌّ منها ACTIVE قبل التحقق الحيّ في Codespaces بالأسرار
  (import + call chain + runtime evidence — §6.6).

---

## 6.5. الدَّين الهندسي + الوكيل المستقلّ (خارطة مُوثَّقة — لا تُنسى)

> **مصدر هذا القسم:** نقد هندسي خارجي (مراجعة «مهندس وادي السيليكون»، 2026-07-29) + مسح
> داخلي للقدرات الوكيلية. **كل بند أدناه تحقّقتُ منه بنفسي في الكود** — لم أقبل النقد على
> عِلّاته، ولم أرفضه؛ الحكم بالدليل. الترتيب أدناه هو ترتيب التنفيذ المُقترَح.

### 6.5.أ — حُكم على النقد الخارجي (بالدليل، لا بالانطباع)

| # | ادّعاء النقد | حكمي بعد الفحص | الدليل |
|---|--------------|-----------------|--------|
| 1 | «`No Any` معلن لكن `Any` موجود على حدود التكامل» | ✅ **مؤكَّد — الأخطر** | `app/integration/gateways/research.py:21,22,36,49,51`؛ و**175** موضع `Any` في `app/`. الحدود هي آخر مكان يُقبل فيه `Any`. |
| 2 | «قائمة تجاهل ruff متسامحة» | ✅ **مؤكَّد** | `pyproject.toml:15-33` يُسكت `PLR0911/0912/0915/2004` + `PLW0603` — أي حُرّاس التعقيد والدوالّ المتضخّمة. |
| 3 | «لا عقود OpenAPI لكل خدمة» | ❌ **خاطئ — مُغلَق مسبقاً** | 13 عقداً مُلتزَماً في `docs/contracts/openapi/` تحرسها `check_openapi_parity.py` (**13/13**، D-173/174/185). النقد نظر إلى `docs/contracts/` السطح الأعلى ففاته المجلّد. |
| 4 | «`X-Correlation-ID` غير مضمون على النداءات الصادرة» | ⚠️ **مؤكَّد جزئياً** | **31** استعمالاً مباشراً لـ`httpx.AsyncClient(` مقابل **21** حقناً للترويسة ⇒ فجوة حقيقية. لكن `check_tracing_gate.py` موجودة (تغطية جزئية). |
| 5 | «`kernel.py` يبتلع أخطاء التتبّع» | ✅ **مؤكَّد** | `app/kernel.py:161,188` — `except Exception` + `logger.debug` فقط. مقبول في التطوير، خطر في الإنتاج («الطيران أعمى»). |
| 6 | «`mypy` ليس في CI» | ✅ **مؤكَّد** | صفر إشارة لـ`mypy` في `.github/workflows/*.yml`؛ و`AGENTS.md` يعترف بذلك صراحةً. |
| 7 | «خطر المونوليث الموزَّع» | ⚠️ **مؤكَّد كخطر، لا كواقع** | الحدود مفروضة فعلاً (`check_no_cross_service_imports` · `check_no_app_imports_in_microservices` · catalog + ports). لكن استقلال **النشر** غير مُثبَت لكل خدمة. |
| 8 | «`app/` سمين — بوّابة أم نواة؟» | ⚠️ **مؤكَّد كسؤال حدود** | `RealityKernel` يتولّى schema + admin bootstrap + telemetry — أكبر من «بوّابة». يحتاج تسمية حدود صريحة، لا بالضرورة تفكيكاً. |
| 9 | «الوثائق ليست قابلة للتنفيذ» | ⚠️ **مؤكَّد جزئياً** | 14+ بوّابة `scripts/fitness/*` تُنفِّذ الدستور فعلاً (وهذا أقوى ممّا افترض النقد)، لكن البنود 1/2/4/6 بلا بوّابة. |

**الخلاصة الصادقة:** جوهر النقد صحيح — **الفجوة بين الدستور المكتوب والفرض الآلي**. لكنه أقلّ
حدّة ممّا صُوِّر: العقود والحدود مفروضة آلياً بالفعل. البنود المفتوحة الحقيقية أربعة:
**`Any` على الحدود · تجاهلات ruff · `mypy` غائب · حقن `Correlation-ID` غير مُلزَم**.

### 6.5.ب — ✅ الثغرة الأمنية أُغلقت (D-187 · M0 مُنفَّذ)

**كان** `app/services/agent_tools/shell_tool.py`:
- `ALLOWED_COMMANDS` **مُعرَّفة ولا تُستخدَم إطلاقاً** — قائمة سماح ميتة.
- الحارس الفعلي **قائمة منع** + تنفيذ بـ**`shell=True`**. مسبار حيّ أثبت مرور الثمانية:
  `;` · `|` · `$(...)` · `` `...` `` · سطر جديد · `awk` (خارج القائمة) · هروب مسار إلى
  `/etc` · خروج `curl` إلى الإنترنت. و`echo $(id -u)` **أعاد `0`**.
- وبابان آخران: `git_tool` (`args` بلا تحقّق) و`testing_tool` (f-string + `shell=True`).

**صار** (D-187): `agent_tools/sandbox/` — `argv` + `shell=False` · قائمة السماح **مفروضة**
· سجن مسارات بـ`resolve()` · `RLIMIT_*` + `setsid` · سقف مخرَج · بلا شبكة افتراضاً.
والمستهلك الحيّ الوحيد (عدّ الملفات) صار بايثون خالصة، فحُذِف `_execute_shell_tool`
ولم يعد لمسار الطالب طريقٌ إلى التنفيذ إطلاقاً. تحرسه بوّابة `check_no_shell_true.py`
(دَينها المُجمَّد **فارغ**) وبطاريةُ هجومٍ من 22 اختباراً.

**يبقى الحدّ قائماً:** ممنوع توصيل أي مُخطِّط/LLM بالأدوات قبل استكمال M1→M4 (عقد
الأدوات + القدرات الحيّة + الحلقة + الحوكمة). القدرة ≠ الأمان.

### 6.5.ج — خارطة الوكيل المستقلّ (Agentic Runtime)

**المبدأ الحاكم:** القدرة ≠ الأمان (P2 في `agentic_runtime_doctrine.md`). القدرة تُضاف بالكود،
والأمان يُفرَض ببوّابة حتمية — لا بنصٍّ «ينبغي على النموذج أن يلتزم به».

**حدّان لا يُخترَقان:**
1. **الوكيل أداة مهندسين لا أداة طلاب.** مسار الطالب (`/api/chat/ws`) ممنوع بنيويّاً أن يبلغ
   تنفيذ shell أو كود — تفرضه بوّابة CI. خلط التنفيذ بمحرّك التدريس يفتح سطح هجوم على 800 ألف
   طالب بلا أي مكسب تربوي.
2. **الوكيل يقترح ولا يدمج.** التحسين الذاتي يفتح PR ويترك `required-ci` حكماً (فلسفة
   CI truth-gate). ممنوع: الدفع إلى `main` · تعديل بوّابات CI · تعطيل اختبار.

**ما يملكه المستودع فعلاً اليوم** (مسح مُتحقَّق منه): أدوات fs · git · shell · testing · search ·
memory · structural (`app/services/agent_tools/`) · MCP بـ8 أدوات · `TOOL_REGISTRY` في
الأوركستريتور — لكن في **ثلاثة سجلّات منفصلة بلا عقد موحَّد**. و`runtime_truth.py` + §6.6 هي
عمليّاً «كشف القدرات» ينقصها أن تصير مسباراً حيّاً.

| المرحلة | المخرَج | معيار الخروج |
|---------|---------|--------------|
| **M0 — المُنفِّذ الآمن** ✅ **(D-187)** | `agent_tools/sandbox/`: argv + `shell=False` · قائمة السماح **مفروضة** على `argv[0]` · سجن مسارات بـ`resolve()` (يتبع الروابط) + فحص وسائط المسارات · `RLIMIT_CPU/AS/NPROC/FSIZE` + `setsid` · سقف مخرَج على البايتات · بلا شبكة افتراضاً (`curl`/`wget` خارج القائمة) · بيئة صريحة لا تُورَّث | ✅ 22 اختبار هجوم كلّها تُرفَض (مُثبَتة حمراء قبل الإصلاح بمسبار حيّ) · صفر انحدار في `_count_files_in_project` (48030 / 28453 مطابقة رقميّاً) · بوّابة `check_no_shell_true` بدَين مُجمَّد **فارغ** |
| **M1 — عقد الأدوات الموحَّد** | `shared/tool_contract/` (نمط `shared/notation` المُثبَت): لكل أداة `input/output schema` + **`capability`** (READ_ONLY · WRITE_WORKSPACE · EXEC_SANDBOX · NETWORK · DESTRUCTIVE) + `approval`. adapters تلفّ السجلّات الثلاثة | كل أداة مُصنَّفة؛ بوّابة تمنع سجلّاً رابعاً |
| **M2 — القدرات الحيّة** | `GET /capabilities` — ترقية `runtime_truth` من فحص ساكن إلى مسبار حيّ | الوكيل يرفض التخطيط لأداة غير مُثبَتة حيّاً |
| **M3 — الحلقة** | `agent-runtime-service :8012` (API-first ⇒ **14/14**): `Intake → CapabilityProbe → Planner → PolicyGate → Executor → Observer → Critic → (replan\|finish)` + ميزانيات صارمة (خطوات · زمن · نداءات · توكنات · كلفة) + `agent_audit` **append-only** | مهمّة حقيقية E2E؛ استنفاد ميزانية ⇒ **إطار نهائي واحد** لا تعليق (§6.5) |
| **M4 — الحوكمة + التحسين الذاتي** | تدفّق موافقة بشرية لـ`DESTRUCTIVE`/`NETWORK` + `check_agent_self_modification.py` + لوحة Grafana | محاولة تعديل ذاتي لبوّابة ⇒ **CI أحمر** (تجربة سلبية) |

**`Critic` يُحقّق `CritiqueNode`** المُخطَّط منذ D-109 — والكاتب لا يُصحّح نفسه (مبدأ P4).

### 6.5.ه — درسٌ منهجي مُكتسَب بالدليل (D-190 · 2026-07-30)

**بطارية لا تفحص الصلة تُصادق على كارثة.** في هذه الجولة كُتِبت بطارية «يجيب على كل سؤال»
تفحص **البنية** (إطار نهائي واحد · عربي · صفر انهيار) فأعطت **8/8**. وعند تشديد المعيار إلى
**الصلة الموضوعية** صارت **7/9** وظهرت أربع كوارث حقيقية (سؤال فيزياء يتلقّى قائمة تشخيص
الاحتمالات · سؤال تكامل يتلقّى تمريناً مخزَّناً آخر · وعدٌ بشرح بصري بلا حمولة · أرقام تمرين
تُزيح أرقام الطالب).

**قاعدتان دائمتان لكل تحقّق لاحق:**
1. أي بطارية «يجيب على كل سؤال» **يجب** أن تفحص صلة الإجابة بالسؤال، لا شكلها. البنية السليمة
   مع محتوى خاطئ أسوأ من الفشل الصريح — لأنها تُنتج تقريراً أخضر كاذباً.
2. **التحليل الساكن لا يكفي لتشخيص التوجيه.** جذر الكارثة الأخطر (عدّاد الحيرة يقرأ نسخة الدور
   الحاضر من التاريخ) لم يظهر إلا بالتعقّب **وقت التشغيل**؛ التحليل الساكن أفاد مرّتين أن كل
   شروط بوّابة الدخول False.

### 6.5.د — سدّ الدَّين الهندسي (يوازي الوكيل)

| # | البند | الإجراء المُقترَح | لماذا الآن |
|---|-------|-------------------|-------------|
| D1 | `Any` على الحدود | ✅ **مُنفَّذ (D-192)**: `app/integration/{protocols,gateways}` مُصنَّفة بنماذج `app/integration/models.py` (**22 → 0**)، وبوّابة `check_no_new_any` بحدٍّ صفريّ على `app/integration/` ودَينٍ مُجمَّد **141** (AST، ثنائي الاتجاه) | حدود التكامل هي أخطر مكان لفقد أمان العقد — وقد كانت البوّابة **تُوسّع** ما ضيّقه العميل |
| D2 | `mypy` غائب | ✅ **مُنفَّذ (D-192)**: وظيفة `Mypy` في `ci.yml` على `shared/` + `app/integration/` (`--follow-imports=silent`، مثبَّت `1.8.0`). **أوّل تشغيل التقط عطباً حقيقياً**: `planning_client.generate_plan` تابعٌ غير موجود (العميل يملك `create_plan`) — كل نداء للبوّابة كان ينتهي بـ`AttributeError`. النطاق يتوسّع بسداد الدَّين لا بتخفيف `mypy.ini` | بلا `mypy` يبقى «strict typing» شعاراً |
| D3 | تجاهلات ruff | ✅ **مُنفَّذ (D-192)**: `PLR0911/0912/0915` أُزيلت من التجاهل العامّ، و`[tool.ruff.lint.per-file-ignores]` (لم تكن موجودة) تُجمِّد **110** ملفّاً مخروقاً. الكود الجديد محكوم فوراً (مُثبَت بمسبار) | يمنع «god functions» من النمو بلا كسر العالم |
| D4 | `Correlation-ID` | ✅ **مُنفَّذ (D-189)**: `shared/http_client/correlated.py` يحقن الترويسة + `traceparent` مشتقّاً + مهلة صريحة إلزامية؛ المونوليث يُسجِّل `ContextVar` القائم (بلا مصدر سادس)؛ الشوط الأول مُهاجَر (`memory_client` · `auditor_client` · `notation_skill`)؛ بوّابة `check_correlated_http` بـratchet ثنائي الاتجاه ودَين مُجمَّد **24**. **يبقى:** توريد محروس بتكافؤ للخدمات المصغّرة (نمط D-185) + حقن لكل-طلب للعملاء طويلي العمر | كان 31 استعمالاً مباشراً مقابل 21 حقناً = تتبّع مثقوب. والأسوأ: مواضع الحقن تُولِّد `uuid4()` جديداً فتقطع السلسلة بدل مدّها |
| D5 | فشل التتبّع الصامت | سياسة حسب البيئة: تطوير ⇒ تحذير · إنتاج ⇒ فشل إقلاع صريح | «Degraded ≠ Dead» مطبَّقاً على الرصد نفسه |
| D6 | حدود `app/` | 🟡 **مُسيَّج لا مُفكَّك (D-192)**: بوّابة `check_router_domain_logic` تُجمِّد **13** استدعاء نطاق في **4** مُوجِّهات (تتقلّص فقط). التفكيك مؤجَّل عمداً: `chat_stream_ws` (662 سطراً) أخطر مسار حيّ، ولا يُفكَّك في جولة تحمل إصلاحاً تربوياً وأمنياً | يمنع أن يصير `app/` مونوليثاً فوق الخدمات |
| **D7** | **ازدواج علامات النيّة المُجمَّد** (D-186) | تقليص `_FROZEN_DEBT` في `scripts/fitness/check_intent_single_source.py` مدخلاً مدخلاً. الأولوية للمرشَّحات الصريحة: `escape_hatch` (كاشف السؤال المفاهيمي) · `bkt_engine` (إشارة evidence) · `probability_vocab` (كاشف الإحباط) — كلها في المسار التعليمي الحيّ. أمّا نسخ الخدمات المصغّرة فتُحسم بقرار: توريد محروس بتكافؤ (نمط D-185) أم استهلاك مباشر | الازدواج **مرئي ومحصور** الآن (البوّابة تمنع السادسة وتمنع تضخّم الخريطة)، لكنه لم يُسدّ. جذر ISS-128/138/139 واحد، وكل مدخل باقٍ هو موضع كارثة محتملة |

**قاعدة الترتيب:** **M0 قبل كل شيء** (إصلاح أمني لكود حيّ، لا ميزة) ⇒ ثم M1 ⇒ ثم D1/D4
(أعلى أثر أمني/تشغيلي) ⇒ ثم M2/M3 ⇒ ثم D2/D3/D5/D6/**D7** ⇒ ثم M4.
(D7 يتقلّص انتهازياً مع أي مسّ لتلك الملفّات — لا ينتظر دوره؛ فقاعدة «يتقلّص فقط» تجعل
كل مرور على ملفٍّ فرصةَ سداد.)

---

## 7. مقاييس النجاح (كيف نعرف أننا انتصرنا)

| المقياس | التعريف | الاتجاه المطلوب |
|---------|---------|-----------------|
| **فجوة الوهم** | assisted_mastery − unaided_delayed_mastery | ↓ نحو الصفر |
| **الإتقان غير المدعوم المؤجَّل** | قدرة الطالب على الحلّ بلا دعم بعد فترة | ↑ |
| **نسبة التوليد/التعرّف** | كم خطوة ولّدها الطالب مقابل كم قرأها | ↑ التوليد — **D-155**: التشخيص قبل المحتوى (probe بلا قيم) + الإجابة المولَّدة تُعترف وتتقدّم (`numeric_verdict` في سجلّ `socratic_evaluation`) |
| **معدّل كشف النتيجة خارج وضع التحقق** | تسريبات `$$\boxed{}$$`/`P(A)=رقم` للطالب | = 0 (صفر مطلق) — **مُنفَّذ بنيوياً في البُناة الحتمية (D-154)**: كل الذيول أسئلة توليد «ركّب الاحتمال بنفسك»، النسبة النهائية لا تُطبع أبداً |

**ممنوع** التحسين على: مدة الجلسة، عدد الرسائل، «الرضا» اللحظي — كلها مؤشرات وهم طلاقة.

---

## 8. مراجع

- **السرد الكامل**: CLAUDE.md §6.91 (M1) → §6.96 (M2) → §6.97 (M4/M5).
- **القرارات**: `.memory/decisions.md` — D-104, D-111, D-112, D-113 (ج1+ج2).
- **الكوارث المُصلَحة**: `.memory/issues.md` — ISS-112, ISS-115.
- **فلسفة الـ Skills**: CLAUDE.md §0.5 + `app/services/skills/doctrine.py`.
- **منصّة الـ Skills الموحَّدة**: §6.87 (D-100) — registry + `/api/v1/skills`.
- **البحث الأساس**: «وَهْم الإتقان» (Bjork / Roediger / Kapur / Renkl).


### M12 — Pedagogical Policy Consolidation (D-144)
Formalized `PedagogicalPolicyEngine` orchestrating `DialogueManagerSkill` through rich `TutorState`.

### M13 — الكيانات المهيكلة قبل الاستخراج من النثر (Structured Entities @ Billions Scale — D-153)
> الطموح: ملايير التمارين. الدرس الدستوري: ISS-120 («بطاقة رقم 0» الوهمية من نثر الحل النموذجي).
- **الهدف**: تركيبة كل تمرين وحقائقه الرمزية تأتي من **`parsed_entities` المهيكلة** في Supabase
  `bac_exercises` (موجودة فعلاً للتمارين الثلاثة المُهاجَرة) — استخراج النثر (`_extract_count_entities`)
  يصبح fallback مُقيَّداً للمحتوى غير المهيكل فقط، ولا يرى نثر الحل أبداً (`load_exercise_questions_only`).
- **دَين قابلية التوسّع المعروف**: `_load_canonical_combinations` وأشقاؤه يستدعون
  `detect_exercise_retrieval(question="اعطني تمرين الاحتمالات 2024")` — سؤال قانوني **مُثبَّت حرفياً**
  لتمرين واحد. التعميم = اشتقاق التمرين النشط من `tutor_state.active_concept`/الـ history ثم قراءة
  `parsed_entities` من الـ DB (عبر مسار retrieval المُفهرَس القائم — D-099).
- **DKT (Deep Knowledge Tracing)**: طبقة تحليلات مخطَّطة فوق BKT ثنائي القناة (D-126) — **PLANNED**،
  لا تُبنى قبل أن تُثبت مقاييس فجوة الوهم الحاجة (قاعدة «القياس قبل التوسّع»).
- **الحارس**: بوّابة `scripts/fitness/check_pedagogical_os.py` (D-153) تفرض قواعد الدستور بدءاً من الآن.
- **حالة تفكيك التعقيد (2026-07-14 — D-163→D-166)**: God-file المونوليث **انتهى** —
  `orchestrator_client.py` 6,154 → **267 سطراً** (قشرة نقل؛ العقل + 6 mixins خلف مانيفست
  `TUTOR_SOURCE_FILES`). **الهدفان المؤجَّلان أُنجزا (D-168، 2026-07-16 — §6.141)**:
  `routes.py` **3,312 → 1,561 سطراً (−53%)** خلف مانيفست `api_sources.API_SOURCE_FILES`
  (6 وحدات chat_types/chat_context/identity_access/stream_serialization/conversation_store/
  chat_stream_engine — الـ handlers بقيت في routes فبوّابة الـ AST بلا تغيير) +
  `probability_tutor_brain.py` **2,441 → جذر تركيب ~55 سطراً + 5 mixins متجاورة** خلف
  مانيفست `brain_sources.BRAIN_SOURCE_FILES` (فحص CI: `BRAIN ⊆ TUTOR`). قاعدة
  **monkeypatch late-binding** (رقِّع وحدة المستدعي) صارت عقداً دائماً — §6.141.
- **تفكيك التعقيد اكتمل (2026-07-18 — D-170، §6.143)**: آخر الملفات الضخمة فُكِّكت —
  `chat_with_agent` **1,926 → 440 سطراً** (13 مرحلة sub-generator فوق `TurnContext`،
  6 وحدات turn_* مُضافة لـ `TUTOR_SOURCE_FILES`)، `local_graph.py` **1,769 → 1,193**
  (مُطهِّرات + شرح مُستخرَجان + re-export خلفي)، `probability_skill.py` **1,685 → 1,462**
  (نماذج Pydantic → `probability_models.py`). النقل حرفي، سلوك مطابق بالبايت.
  **ISS-132 مُغلَقة (D-171)**: هوية الإدمن تُصرَّح في `AgentState` + تُمرَّر مُسوَّرةً بـ
  `_is_admin_payload` فتعبر الرسم الـ12-node — أدوات الإدمن تعمل عبر `/api/chat/messages`.
