| Category | Atomic Issue Detected | Exact File & Line Range | Risk Level (P0-P3) | Root Cause (AST-Level) | Microscopic Surgical Fix |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **Arch** | Split-Brain Legacy Duplicate | `microservices/orchestrator_service/src/services/overmind/` | **P1** | Microservice directory duplicates `app/services/overmind`, violating Strangler isolation. | Delete legacy `app/` overmind and force microservice API routing. |
| **Async** | Synchronous CPU bound Regex | `microservices/orchestrator_service/src/services/overmind/agents/auditor.py:356-386` | **P2** | Heavy regex `re.sub` inside `async def` agent node without executor offloading blocks event loop. | Offload CPU-bound string manipulation to `run_in_executor`. |
| **Async** | Synchronous Blocking `json.loads` | `microservices/orchestrator_service/src/services/overmind/agents/auditor.py:323,327` | **P1** | Synchronous JSON parse blocks event loop inside `async def` agent node. | Delegate `json.loads` to threaded executor or fast async JSON parser. |
| **Async** | Background Task GC Leak | `microservices/orchestrator_service/src/services/overmind/entrypoint.py:39` | **P0** | `_task = asyncio.create_task()` assigns to local variable, garbage collected mid-execution. | Track task in global `BackgroundTasks` set to prevent GC. |
| **Async** | WebSocket Proxy Event-Loop Blocking | `microservices/api_gateway/websockets.py:96` | **P0** | `asyncio.wait([asyncio.create_task(...)])` blocks proxy loop heartbeat on upstream IO. | Decouple client read/write loops using queues and independent tasks. |
| **LangGraph** | Mutable State Pointer | `microservices/orchestrator_service/src/services/overmind/langgraph/engine.py:387` | **P1** | `context.update()` mutates `shared_memory` which holds pointer to LangGraph state dict. | Deepcopy `state` objects before injecting into `CollaborationContext`. |
| **LangGraph** | Split/Double Iteration Increment | `microservices/orchestrator_service/src/services/overmind/langgraph/engine.py:514,591` | **P1** | Both `_supervisor_node` and `_loop_controller_node` increment the iteration counter. | Remove increment from `_supervisor_node` and track only in explicit loop controller. |
