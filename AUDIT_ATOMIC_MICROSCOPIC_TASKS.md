# Atomic Microscopic Tasks

| Issue | Exact File/Line | Risk Level | Microscopic Fix |
|---|---|---|---|
| TLS certificate validation is explicitly disabled for `sslmode=require`, enabling MitM on DB transport. | app/core/database.py:109 | P0-Critical | Replace `ctx.check_hostname = False` with `ctx.check_hostname = True`. |
| TLS certificate validation is explicitly disabled by setting `CERT_NONE` for `sslmode=require`, enabling MitM on DB transport. | app/core/database.py:110 | P0-Critical | Replace `ctx.verify_mode = ssl.CERT_NONE` with `ctx.verify_mode = ssl.CERT_REQUIRED`. |
| Multiple gateway compatibility routes proxy requests without JWT verification dependency, allowing unauthenticated access to protected upstream endpoints. | microservices/api_gateway/main.py:523 | P0-Critical | Add `dependencies=[Depends(verify_gateway_request)]` to the `@app.api_route("/admin/{path:path}", ...)` decorator. |
| Multiple gateway compatibility routes proxy requests without JWT verification dependency, allowing unauthenticated access to protected upstream endpoints. | microservices/api_gateway/main.py:543 | P0-Critical | Add `dependencies=[Depends(verify_gateway_request)]` to the `@app.api_route("/api/security/{path:path}", ...)` decorator. |
| Multiple gateway compatibility routes proxy requests without JWT verification dependency, allowing unauthenticated access to protected upstream endpoints. | microservices/api_gateway/main.py:562 | P0-Critical | Add `dependencies=[Depends(verify_gateway_request)]` to the `@app.api_route("/api/chat/{path:path}", ...)` decorator. |
| Multiple gateway compatibility routes proxy requests without JWT verification dependency, allowing unauthenticated access to protected upstream endpoints. | microservices/api_gateway/main.py:581 | P0-Critical | Add `dependencies=[Depends(verify_gateway_request)]` to the `@app.api_route("/v1/content/{path:path}", ...)` decorator. |
| Multiple gateway compatibility routes proxy requests without JWT verification dependency, allowing unauthenticated access to protected upstream endpoints. | microservices/api_gateway/main.py:608 | P0-Critical | Add `dependencies=[Depends(verify_gateway_request)]` to the `@app.api_route("/api/v1/data-mesh/{path:path}", ...)` decorator. |
| Multiple gateway compatibility routes proxy requests without JWT verification dependency, allowing unauthenticated access to protected upstream endpoints. | microservices/api_gateway/main.py:628 | P0-Critical | Add `dependencies=[Depends(verify_gateway_request)]` to the `@app.api_route("/system/{path:path}", ...)` decorator. |
| Gateway WebSocket proxy logs full client URL, which can include query-token material and leak JWTs into logs. | microservices/api_gateway/websockets.py:66 | P1-High | Replace `logger.info(f"API_GATEWAY received query (websocket: {client_ws.url})")` with `logger.info("API_GATEWAY received websocket request")`. |
| `StateGraph` is compiled with in-memory checkpointer in orchestrator startup path, causing loss of durable state in production. | microservices/orchestrator_service/main.py:88 | P1-High | Replace `admin_graph.compile(checkpointer=MemorySaver(), interrupt_before=[])` with `admin_graph.compile(checkpointer=postgres_checkpointer, interrupt_before=[])` after injecting a PostgreSQL-backed checkpointer instance. |
| Bootstrap compiles `StateGraph` with `MemorySaver`, violating production durability requirements for LangGraph checkpoints. | microservices/orchestrator_service/src/core/bootstrap.py:44 | P1-High | Replace `graph.compile(checkpointer=MemorySaver())` with `graph.compile(checkpointer=postgres_checkpointer)` using a PostgreSQL-backed checkpointer dependency. |
| `async def execute_command` performs blocking `subprocess.run(...)` directly in the event loop thread. | microservices/orchestrator_service/src/services/overmind/capabilities/shell_operations.py:95 | P1-High | Replace `result = subprocess.run(...)` with `result = await asyncio.to_thread(subprocess.run, args, check=False, shell=False, cwd=cwd, timeout=timeout, capture_output=True, text=True)`. |
| Gateway maintains mutable module-level shared state (`legacy_ws_sessions_total`) that is process-global and not service-bound, risking cross-request race inconsistencies. | microservices/api_gateway/main.py:29 | P2-Medium | Replace `legacy_ws_sessions_total: dict[str, int] = {}` with `legacy_ws_sessions_total: collections.Counter[str] = collections.Counter()` guarded by an `asyncio.Lock` during mutation. |
| Chat workflow has supervisor↔worker cycles with no explicit iteration/delegation cap, allowing unbounded routing loops when supervisor never emits `FINISH`. | app/services/chat/graph/workflow.py:121 | P2-Medium | Add an iteration guard in state and route `supervisor` to `END` when `state["iteration"] >= state["max_iterations"]`. |
| Planning graph compiles without explicit persistent checkpointer, losing resumability and replay guarantees for production runs. | microservices/planning_agent/graph.py:161 | P2-Medium | Replace `graph = workflow.compile()` with `graph = workflow.compile(checkpointer=postgres_checkpointer)` using a PostgreSQL-backed checkpointer instance. |
| WebSocket chat loop lacks heartbeat/idle timeout control, allowing infinite idle connections and unbounded resource retention. | microservices/orchestrator_service/src/api/routes.py:767 | P2-Medium | Replace `incoming = await websocket.receive_json()` with `incoming = await asyncio.wait_for(websocket.receive_json(), timeout=30.0)` and on timeout send ping/close. |
| WebSocket admin chat loop lacks heartbeat/idle timeout control, allowing infinite idle connections and unbounded resource retention. | microservices/orchestrator_service/src/api/routes.py:865 | P2-Medium | Replace `incoming = await websocket.receive_json()` with `incoming = await asyncio.wait_for(websocket.receive_json(), timeout=30.0)` and on timeout send ping/close. |
| WebSocket mission loop lacks heartbeat/idle timeout control, allowing infinite idle connections and unbounded resource retention. | microservices/orchestrator_service/src/api/routes.py:1218 | P2-Medium | Replace `incoming = await websocket.receive_json()` with `incoming = await asyncio.wait_for(websocket.receive_json(), timeout=30.0)` and on timeout send ping/close. |
