# E-TAALEEM — GOD-TIER ATOMIC ARCHITECTURAL AUDIT & SRE PROTOCOL v2.0

| Layer | Atomic Vulnerability | File:Line | Impact (P0-P3) | Root Cause Evidence | Microscopic Surgical Fix |
|-------|----------------------|-----------|----------------|---------------------|--------------------------|
| RAG / Event Loop | Blocking vector retrieval + reranker executed inline inside `async def` (no thread offload) | `microservices/research_agent/src/search_engine/strategies.py:53` | P1 | `retriever.search(...)` and `reranker.rerank(...)` are synchronous CPU/network-heavy calls inside async path, so they can stall the event loop under load. | Wrap both in worker offload: `nodes = await anyio.to_thread.run_sync(retriever.search, query, retrieval_limit, filters)` and `nodes = await anyio.to_thread.run_sync(reranker.rerank, query, nodes, limit)`; keep async method non-blocking. |
| Stream Integrity / Secretary Pattern | Frontend refresh can run before local DB assistant-write completes (stale-refetch race) | `frontend/app/hooks/useAgentSocket.js:122` + `app/api/routers/customer_chat.py:220` + `app/api/routers/customer_chat.py:254` | P0 | Streamed `complete/assistant_final` is forwarded immediately to UI and triggers refresh; local assistant persistence happens later in `finally`, so UI can refetch stale conversation snapshot. | Delay emitting terminal event until after local commit, or emit a dedicated `persisted` event from backend and refresh only on that (`if (type === "persisted") refreshConversationHistory();`). |
| Data Integrity / Postgres Guardrail | Null-byte sanitization not enforced before every DB insert (user messages unsanitized) | `app/api/routers/customer_chat.py:158` + `app/api/routers/admin.py:231` | P0 | Assistant content is sanitized (`replace("\x00","")`), but user `question` is persisted raw in both routes; a single `\x00` can still trigger DB failures. | Add centralized sanitizer and enforce on all writes: `clean_question = question.replace("\\x00", "")` before every `save_message(...)` call (user + assistant + any metadata fields). |
| Multi-Agent / MCP Tool Safety | MCP/kernel tool invocations lack mandatory timeout, idempotency key, and circuit-breaker envelope | `app/core/integration_kernel/runtime.py:52` | P1 | `run_workflow/search/optimize/rank/act` directly await driver calls with no timeout guard, no idempotency tracking, no breaker state; external tool failures can cascade. | Introduce a guarded executor wrapper: `await resilience.execute_safely(operation_name, func, timeout_ms=..., idempotency_key=..., circuit_breaker=...)` and route all kernel calls through it. |
| LangGraph Determinism | Shared state aliasing enables in-place mutation drift across nodes | `microservices/orchestrator_service/src/services/overmind/langgraph/engine.py:298` + `microservices/orchestrator_service/src/services/overmind/domain/context.py:24` | P1 | `_build_context` passes `state["shared_memory"]` by reference; `context.update(...)` mutates same dict in place, breaking pure-state transition guarantees and idempotent replay behavior. | Copy on read/write: `InMemoryCollaborationContext(dict(state.get("shared_memory", {})))` and return a fresh immutable update object from each node. |
| Redis / Scalability | Full keyspace materialization on scan path (O(N) memory spike) | `app/caching/redis_cache.py:205` | P2 | `scan_iter` results are appended into a full list before return; large keyspaces can cause latency spikes and memory pressure in one request. | Convert to bounded pagination/streaming API: return cursor batches (`SCAN` with count), or enforce `max_keys` cutoff + incremental consumer callback instead of full in-memory accumulation. |
| Frontend Sync Policy | Conversation list refetch is tied to stream events, not socket lifecycle + commit acknowledgment | `frontend/app/hooks/useAgentSocket.js:130` | P1 | Refresh occurs on `complete`/`assistant_final` event (plus timer), not on confirmed backend commit or WebSocket close ack; this violates strict “read only after stream close + commit” guardrail. | Gate refetch behind backend-confirmed commit token (`payload.persisted_at` or `db_commit_seq`) and enforce monotonic read (`if (!payload.persisted) skip refresh`). |
| RAG Performance Ceiling | No Redis cache layer for repeated vector retrieval requests in retrieval orchestrator path | `app/services/chat/tools/retrieval/service.py:85` + `app/services/chat/tools/retrieval/remote_client.py:63` | P2 | Every query path calls remote search directly; no request-level/result-level Redis memoization is applied before hitting vector backend repeatedly. | Add semantic cache key (`hash(query+filters+limit)`) via Redis before remote fetch; cache hits short-circuit remote call and enforce retrieval latency SLO guard (track p95 + alert if >200ms). |
