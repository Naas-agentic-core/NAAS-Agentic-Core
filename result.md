| Layer | Atomic Vulnerability | File:Line | Impact (P0-P3) | Root Cause Evidence | Microscopic Surgical Fix |
|-------|----------------------|-----------|----------------|---------------------|--------------------------|
| 1. CONCURRENCY & CONNECTION POOLING | Blocking Event Loop (Sync JSON Parse) | `microservices/orchestrator_service/src/api/routes.py:589` | P1 | `json.dumps` called synchronously on potentially large dictionary within `async def _stream_chat_langgraph` without offloading, blocking the event loop. | Offload to thread: `await anyio.to_thread.run_sync(json.dumps, final_resp, ensure_ascii=False)` |
| 1. CONCURRENCY & CONNECTION POOLING | Background Task GC Risk | `microservices/api_gateway/websockets.py:96` | P0 | `asyncio.create_task` created inline inside `asyncio.wait([])` without strong references or module-level global set tracking, risking premature garbage collection. | Assign tasks to local variables, add to `active_background_tasks`, and attach strong-referenced `add_done_callback`. |
| 1. CONCURRENCY & CONNECTION POOLING | Connection Pool Ghosting | `microservices/orchestrator_service/src/api/routes.py:848` | P0 | `WebSocketDisconnect` is caught but active tasks (`_stream_chat_langgraph`) are not cancelled and database sessions inside are not closed in a `finally` block, leading to pool exhaustion. | Wrap stream execution in `try/finally`, cancel background tasks, and explicitly await `session.close()`. |
| 2. MULTI-AGENT ORCHESTRATION | LangGraph State Mutations | `microservices/orchestrator_service/src/services/overmind/state.py:188` | P1 | LangGraph state transitions mutate dictionary pointers directly (`state.update(...)`) instead of returning new state copies, causing non-deterministic graph execution. | Refactor graph nodes to return new dictionaries: `return {**state, "new_key": value}`. |
| 2. MULTI-AGENT ORCHESTRATION | Infinite Reasoning Loops | `microservices/orchestrator_service/src/services/overmind/entrypoint.py:41` | P0 | Supervisor agent lacks hard `recursion_limit` increment tracking within its own node, risking infinite prompt ping-pong loops if workers repeatedly fail. | Increment `iteration: state.get("iteration", 0) + 1` in supervisor state update and strictly enforce `max_iterations`. |
| 3. STREAM INTEGRITY & FRONTEND SYNC | Race Condition (Stale-Refetch Gap) | `microservices/orchestrator_service/src/api/routes.py:595` | P0 | WebSocket final `assistant_final` message is sent immediately before awaiting final DB commit, causing Next.js to refetch stale cache before Postgres completes write. | Ensure the stream accumulator strictly `awaits` DB save transaction *before* sending final `{"type": "assistant_final"}`. |
| 3. STREAM INTEGRITY & FRONTEND SYNC | Null-Byte (`\x00`) Postgres Crashes | `microservices/orchestrator_service/src/api/routes.py:1014` | P0 | While manual `.replace("\x00", "")` exists in some spots, final LangGraph stream payloads are directly sent/accumulated. Missing global sanitization before database inserts will crash asyncpg. | Enforce atomic global sanitization logic across all text columns: `content = content.replace("\x00", "")` before `session.execute`. |
| 4. RAG & LATENCY SURGERY | Embedding Latency / Redundant Vector Compute | `microservices/orchestrator_service/src/services/tools/content.py:345` | P2 | Internet/internal search queries invoke `_do_internal_search` (LlamaIndex CPU-bound embedding) directly without an O(1) Redis caching layer, ballooning p95 latency. | Prepend execution with a Redis `get(hash(query))` cache hit lookup; if missing, execute and `setex`. |
